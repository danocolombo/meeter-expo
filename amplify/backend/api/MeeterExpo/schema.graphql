type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  sub: String
  pictureURI: String
}

type MeeterSystem @model @auth(rules: [{allow: public}]) {
  id: ID!
  version: String
  ios_version: String
  android_version: String
  defaultProfileUri: String
}
 