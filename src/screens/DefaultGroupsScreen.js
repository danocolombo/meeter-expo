import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const DefaultGroupsScreen = () => {
    return (
        <View>
            <Text>DefaultGroupsScreen</Text>
        </View>
    );
};

export default DefaultGroupsScreen;

const styles = StyleSheet.create({});
