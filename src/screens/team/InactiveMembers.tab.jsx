import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const InactiveMembers = () => {
    return (
        <View>
            <Text>InactiveMembers</Text>
        </View>
    );
};

export default InactiveMembers;

const styles = StyleSheet.create({});
